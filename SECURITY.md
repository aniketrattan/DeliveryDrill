# Security

DeliveryDrill sends deliberately malformed and adversarial requests. Use it only against systems you own or are explicitly authorized to test. Keep secrets in environment variables; DeliveryDrill does not print them, but avoid committing scenario files containing real credentials. Report security issues privately to the repository maintainers rather than publishing exploit details first.

