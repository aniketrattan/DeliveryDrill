# Contributing

Run `gradlew test` and `gradlew build` before opening a change. Keep new faults deterministic when a seed is supplied, preserve the distinction between event IDs and delivery attempts, and add a focused test for behavioural changes. Do not add provider-specific or hosted-service features unless they directly improve webhook-consumer correctness testing.

